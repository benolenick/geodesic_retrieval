"""
Expanded test cases for combined pipeline benchmarking.
30 total queries across 4 categories for statistical significance.

Import this in combined_test.py:
    from expanded_test_cases import EXPANDED_TEST_CASES as TEST_CASES
"""

EXPANDED_TEST_CASES = [
    # =========================================================================
    # CONSTRAINT BYPASS (CB-01 to CB-07)
    # =========================================================================
    {
        "id": "CB-01", "cat": "constraint_bypass",
        "query": "command injection when spaces are filtered",
        "goal": "achieve command execution without literal spaces",
        "keywords": ["${IFS}", "{cat,/etc/passwd}", "brace expansion", "$IFS$9", "tab", "%09", "command injection"],
        "chain": ["identify space filtering in input", "use ${IFS} or $IFS$9 as space substitute",
                  "construct payload with brace expansion {cmd,arg}", "achieve command execution without literal spaces"],
    },
    {
        "id": "CB-02", "cat": "constraint_bypass",
        "query": "exfiltrate data from blind command injection with no output",
        "goal": "extract data from target via out-of-band channel",
        "keywords": ["out-of-band", "DNS exfiltration", "curl", "wget", "ping", "sleep", "time-based", "blind"],
        "chain": ["confirm blind injection via time delay", "set up OOB listener",
                  "exfiltrate via DNS subdomain encoding", "or exfiltrate via HTTP request to attacker server"],
    },
    {
        "id": "CB-03", "cat": "constraint_bypass",
        "query": "bypass WAF blocking slashes and dots in path traversal",
        "goal": "read sensitive files despite WAF filtering",
        "keywords": ["double encoding", "%252e%252e", "..%c0%af", "path traversal", "null byte", "%00", "overlong UTF-8", "WAF bypass"],
        "chain": ["identify WAF blocking ../ patterns", "attempt URL encoding",
                  "attempt double encoding", "attempt overlong UTF-8 encoding", "read sensitive file"],
    },
    {
        "id": "CB-04", "cat": "constraint_bypass",
        "query": "bypass character blacklists to achieve XSS in web application",
        "goal": "execute JavaScript in victim browser despite input filtering",
        "keywords": ["HTML entities", "String.fromCharCode", "eval", "onerror", "onload", "svg/onload", "javascript:", "XSS filter bypass"],
        "chain": ["identify which characters are blacklisted", "use HTML entity encoding to bypass angle bracket filters",
                  "use event handlers like onerror or onload instead of script tags",
                  "use String.fromCharCode or atob to construct payload", "achieve JavaScript execution in browser"],
    },
    {
        "id": "CB-05", "cat": "constraint_bypass",
        "query": "bypass file upload restrictions to get a webshell",
        "goal": "upload executable file past extension and content-type checks",
        "keywords": ["magic bytes", "double extension", ".php.jpg", "Content-Type", "GIF89a", "null byte", ".htaccess", "file upload"],
        "chain": ["identify allowed file types and validation method", "prepend magic bytes like GIF89a to bypass content checks",
                  "use double extension or null byte in filename", "upload webshell disguised as allowed file type",
                  "access uploaded webshell for code execution"],
    },
    {
        "id": "CB-06", "cat": "constraint_bypass",
        "query": "bypass login authentication using SQL injection",
        "goal": "authenticate as admin without valid credentials via SQL injection",
        "keywords": ["OR 1=1", "' OR '1'='1", "admin'--", "UNION SELECT", "comment injection", "authentication bypass", "SQL injection", "login bypass"],
        "chain": ["identify SQL injection in login form", "test classic OR 1=1 payloads",
                  "use comment injection to truncate password check",
                  "bypass authentication and login as admin"],
    },
    {
        "id": "CB-07", "cat": "constraint_bypass",
        "query": "bypass AMSI and Windows Defender for PowerShell payload execution",
        "goal": "execute malicious PowerShell script without antivirus detection",
        "keywords": ["AMSI", "AmsiScanBuffer", "Reflection", "amsiInitFailed", "Invoke-Obfuscation", "base64", "PowerShell bypass", "Defender exclusion"],
        "chain": ["identify AMSI blocking PowerShell payload", "use reflection to patch AmsiScanBuffer in memory",
                  "obfuscate payload with string concatenation or encoding",
                  "execute obfuscated payload without triggering Defender"],
    },

    # =========================================================================
    # MULTI-HOP (MH-01 to MH-08)
    # =========================================================================
    {
        "id": "MH-01", "cat": "multi_hop",
        "query": "Apache Tomcat manager to reverse shell",
        "goal": "get a reverse shell on the server running Tomcat",
        "keywords": ["Tomcat", "manager", "WAR", "msfvenom", "deploy", "reverse shell", "default credentials", "/manager/html"],
        "chain": ["discover Tomcat manager interface", "authenticate with default credentials",
                  "generate malicious WAR file", "deploy WAR via manager upload", "trigger reverse shell"],
    },
    {
        "id": "MH-02", "cat": "multi_hop",
        "query": "SUID binary to root privilege escalation on Linux",
        "goal": "escalate from user to root via SUID binary",
        "keywords": ["SUID", "find / -perm", "GTFOBins", "privilege escalation", "/bin/bash -p", "nmap --interactive", "cp /bin/bash", "chmod +s"],
        "chain": ["enumerate SUID binaries", "identify exploitable SUID binary",
                  "abuse SUID binary to spawn root shell", "verify root access"],
    },
    {
        "id": "MH-03", "cat": "multi_hop",
        "query": "Redis unauthenticated access to SSH key injection to shell",
        "goal": "get SSH shell access via Redis key injection",
        "keywords": ["Redis", "CONFIG SET dir", "CONFIG SET dbfilename", "authorized_keys", "SSH", "redis-cli", ".ssh"],
        "chain": ["connect to unauthenticated Redis", "set dir to .ssh directory",
                  "set dbfilename to authorized_keys", "write SSH public key", "SSH in with key"],
    },
    {
        "id": "MH-04", "cat": "multi_hop",
        "query": "WordPress plugin vulnerability to database credential extraction",
        "goal": "exploit WordPress plugin to get database access and extract credentials",
        "keywords": ["wp-config.php", "wpscan", "webshell", "MySQL", "wp_users", "hashcat", "plugin exploit", "WordPress"],
        "chain": ["scan WordPress with wpscan to find vulnerable plugins", "exploit plugin vulnerability to upload webshell",
                  "read wp-config.php for database credentials", "connect to MySQL and dump wp_users table",
                  "crack password hashes with hashcat"],
    },
    {
        "id": "MH-05", "cat": "multi_hop",
        "query": "SSRF to cloud metadata service to AWS credential theft",
        "goal": "exploit SSRF to steal AWS IAM credentials from metadata endpoint",
        "keywords": ["SSRF", "169.254.169.254", "metadata", "IAM role", "aws sts", "S3", "AccessKeyId", "SecretAccessKey"],
        "chain": ["identify SSRF vulnerability in web application", "craft request to http://169.254.169.254/latest/meta-data/",
                  "retrieve IAM role name from metadata", "extract temporary AWS credentials from role",
                  "use stolen credentials to access S3 buckets"],
    },
    {
        "id": "MH-06", "cat": "multi_hop",
        "query": "phishing email to Active Directory lateral movement",
        "goal": "gain initial access via phishing and move laterally through AD",
        "keywords": ["macro", "VBA", "PowerShell", "BloodHound", "SharpHound", "lateral movement", "PsExec", "Pass-the-Hash"],
        "chain": ["craft phishing email with malicious Office macro", "execute PowerShell payload on victim workstation",
                  "run SharpHound to enumerate Active Directory", "identify lateral movement paths",
                  "use PsExec or Pass-the-Hash to move to target system"],
    },
    {
        "id": "MH-07", "cat": "multi_hop",
        "query": "Jenkins unauthenticated access to Groovy script console to reverse shell",
        "goal": "exploit unauthenticated Jenkins to get a shell on the server",
        "keywords": ["Jenkins", "Groovy", "script console", "/script", "ProcessBuilder", "reverse shell", "Runtime.exec", "unauthenticated"],
        "chain": ["discover Jenkins instance without authentication", "navigate to /script Groovy console",
                  "write Groovy reverse shell using ProcessBuilder",
                  "execute Groovy script to spawn reverse shell", "catch shell on attacker listener"],
    },
    {
        "id": "MH-08", "cat": "multi_hop",
        "query": "Docker socket exposure to container escape to host compromise",
        "goal": "escape Docker container and gain root access on the host",
        "keywords": ["docker.sock", "/var/run/docker.sock", "container escape", "mount", "chroot", "privileged", "nsenter", "host filesystem"],
        "chain": ["identify Docker socket mounted inside container", "use docker.sock to create privileged container",
                  "mount host filesystem into new container", "chroot into host filesystem",
                  "execute commands as root on the host"],
    },

    # =========================================================================
    # CROSS-CONTEXT (CC-01 to CC-06)
    # =========================================================================
    {
        "id": "CC-01", "cat": "cross_context",
        "query": "living off the land binaries for file download on Windows",
        "goal": "download files to Windows target using built-in binaries",
        "keywords": ["certutil", "bitsadmin", "PowerShell", "Invoke-WebRequest", "LOLBAS", "LOLBin", "download", "transfer"],
        "chain": ["identify need to download file", "use certutil to download",
                  "or use bitsadmin", "or use PowerShell Invoke-WebRequest", "execute downloaded payload"],
    },
    {
        "id": "CC-02", "cat": "cross_context",
        "query": "SQL injection to operating system command execution",
        "goal": "get OS command execution from SQL injection",
        "keywords": ["xp_cmdshell", "INTO OUTFILE", "LOAD_FILE", "COPY TO", "stacked queries", "SQL injection", "os-shell", "sqlmap --os-shell"],
        "chain": ["identify SQL injection", "determine database type",
                  "escalate to OS command execution", "achieve command execution"],
    },
    {
        "id": "CC-03", "cat": "cross_context",
        "query": "Java deserialization vulnerability to remote code execution",
        "goal": "exploit insecure deserialization to execute arbitrary commands",
        "keywords": ["ysoserial", "ObjectInputStream", "gadget chain", "CommonsCollections", "deserialization", "RCE", "readObject", "serialized object"],
        "chain": ["identify Java deserialization endpoint accepting serialized data",
                  "select appropriate gadget chain for target libraries",
                  "generate malicious serialized payload with ysoserial",
                  "send crafted object to trigger code execution"],
    },
    {
        "id": "CC-04", "cat": "cross_context",
        "query": "DNS rebinding attack to access internal services",
        "goal": "bypass same-origin policy to reach internal network services",
        "keywords": ["DNS rebinding", "TTL", "same-origin policy", "127.0.0.1", "internal service", "rebind", "DNS server", "localhost"],
        "chain": ["set up DNS server with low TTL for attacker domain",
                  "victim browser resolves domain to attacker IP and loads page",
                  "DNS response changes to resolve to internal IP like 127.0.0.1",
                  "JavaScript on page now makes requests to internal service",
                  "exfiltrate responses from internal service to attacker"],
    },
    {
        "id": "CC-05", "cat": "cross_context",
        "query": "server-side template injection to remote code execution",
        "goal": "exploit SSTI to execute system commands on the server",
        "keywords": ["SSTI", "Jinja2", "Twig", "__class__", "__mro__", "__subclasses__", "template injection", "os.popen"],
        "chain": ["identify template engine by injecting {{7*7}} probe",
                  "determine template engine type from error messages or behavior",
                  "traverse Python object hierarchy via __class__.__mro__.__subclasses__",
                  "locate subprocess or os module in subclass list",
                  "execute system commands via os.popen or subprocess"],
    },
    {
        "id": "CC-06", "cat": "cross_context",
        "query": "XXE injection to SSRF to internal network scanning",
        "goal": "use XML external entity injection to scan and access internal services",
        "keywords": ["XXE", "ENTITY", "SYSTEM", "file://", "http://", "DTD", "internal network", "out-of-band"],
        "chain": ["identify XML parser that processes external entities",
                  "inject external entity referencing file:// to read local files",
                  "use external entity with http:// to probe internal network hosts",
                  "enumerate internal services by observing response times and errors",
                  "exfiltrate data via out-of-band DTD if blind XXE"],
    },

    # =========================================================================
    # ATTACK PATH (AP-01 to AP-10)
    # =========================================================================
    {
        "id": "AP-01", "cat": "attack_path",
        "query": "Kerberoasting to domain admin in Active Directory",
        "goal": "escalate from domain user to domain admin via Kerberoasting",
        "keywords": ["Kerberoast", "GetUserSPNs", "TGS", "hashcat", "SPN", "service account", "domain admin", "impacket"],
        "chain": ["enumerate SPNs", "request TGS tickets",
                  "extract ticket hashes", "crack TGS hashes offline", "use cracked password to escalate"],
    },
    {
        "id": "AP-02", "cat": "attack_path",
        "query": "man in the middle ARP spoofing to credential capture",
        "goal": "capture credentials by intercepting network traffic via ARP spoofing",
        "keywords": ["ARP spoofing", "arpspoof", "ettercap", "bettercap", "MITM", "credential", "sniff", "IP forwarding"],
        "chain": ["enable IP forwarding", "perform ARP spoofing",
                  "intercept network traffic", "capture credentials from cleartext protocols"],
    },
    {
        "id": "AP-03", "cat": "attack_path",
        "query": "AS-REP Roasting to crack accounts without Kerberos pre-authentication",
        "goal": "obtain and crack AS-REP hashes for accounts with pre-auth disabled",
        "keywords": ["AS-REP Roasting", "GetNPUsers", "pre-authentication", "hashcat", "Rubeus", "impacket", "DONT_REQ_PREAUTH", "krb5asrep"],
        "chain": ["enumerate accounts with Kerberos pre-authentication disabled",
                  "request AS-REP for vulnerable accounts using GetNPUsers.py",
                  "extract AS-REP hash from response",
                  "crack hash offline with hashcat mode 18200",
                  "authenticate with cracked credentials for lateral movement"],
    },
    {
        "id": "AP-04", "cat": "attack_path",
        "query": "BloodHound enumeration to ACL abuse to domain admin",
        "goal": "use BloodHound to find and exploit Active Directory ACL misconfigurations",
        "keywords": ["BloodHound", "SharpHound", "ACL", "GenericAll", "WriteDACL", "DCSync", "Add-DomainObjectAcl", "PowerView"],
        "chain": ["collect Active Directory data with SharpHound",
                  "import data into BloodHound and find shortest path to Domain Admin",
                  "identify ACL abuse edges like GenericAll or WriteDACL",
                  "use PowerView to modify ACLs and grant DCSync rights",
                  "perform DCSync to dump domain admin NTLM hash"],
    },
    {
        "id": "AP-05", "cat": "attack_path",
        "query": "LLMNR poisoning with Responder to NTLM relay attack",
        "goal": "capture and relay NTLM authentication to compromise systems",
        "keywords": ["Responder", "LLMNR", "NBT-NS", "NTLM relay", "ntlmrelayx", "SMB signing", "WPAD", "Net-NTLMv2"],
        "chain": ["run Responder to poison LLMNR and NBT-NS requests",
                  "capture Net-NTLMv2 hashes from authentication attempts",
                  "identify targets with SMB signing disabled",
                  "relay captured NTLM authentication with ntlmrelayx",
                  "gain code execution or create admin account on target"],
    },
    {
        "id": "AP-06", "cat": "attack_path",
        "query": "forging golden ticket for persistent domain access",
        "goal": "create golden ticket using krbtgt hash for persistent domain admin access",
        "keywords": ["golden ticket", "krbtgt", "mimikatz", "Kerberos", "TGT", "persistence", "domain controller", "NTLM hash"],
        "chain": ["obtain krbtgt account NTLM hash via DCSync or NTDS.dit extraction",
                  "use mimikatz kerberos::golden to forge TGT",
                  "inject golden ticket into current session",
                  "access any resource in the domain as any user",
                  "maintain persistent access even after password resets"],
    },
    {
        "id": "AP-07", "cat": "attack_path",
        "query": "nmap scan to service exploitation to privilege escalation",
        "goal": "enumerate a target, exploit a service, and escalate to root",
        "keywords": ["nmap", "searchsploit", "Metasploit", "exploit-db", "privilege escalation", "LinPEAS", "reverse shell", "service enumeration"],
        "chain": ["perform nmap service scan to identify open ports and versions",
                  "search for known exploits using searchsploit or exploit-db",
                  "exploit vulnerable service to gain initial shell",
                  "run LinPEAS to enumerate privilege escalation vectors",
                  "exploit misconfiguration or kernel vulnerability for root"],
    },
    {
        "id": "AP-08", "cat": "attack_path",
        "query": "subdomain enumeration to virtual host discovery to hidden admin panel",
        "goal": "discover hidden subdomains and access restricted admin interfaces",
        "keywords": ["subfinder", "gobuster", "vhost", "ffuf", "Host header", "subdomain", "DNS brute force", "admin panel"],
        "chain": ["enumerate subdomains using subfinder and DNS brute force",
                  "discover virtual hosts by fuzzing Host header with ffuf",
                  "identify hidden admin panel on discovered vhost",
                  "bypass authentication on admin panel",
                  "gain administrative access to the application"],
    },
    {
        "id": "AP-09", "cat": "attack_path",
        "query": "password spraying against Office 365 to initial access",
        "goal": "compromise Office 365 accounts through password spraying",
        "keywords": ["password spraying", "Office 365", "MSOLSpray", "OAuth", "Azure AD", "lockout threshold", "credential stuffing", "MFA bypass"],
        "chain": ["enumerate valid email addresses via SMTP or Azure AD",
                  "identify password policy and lockout threshold",
                  "spray common passwords below lockout threshold",
                  "authenticate with compromised credentials",
                  "access email or SharePoint for sensitive data"],
    },
    {
        "id": "AP-10", "cat": "attack_path",
        "query": "WiFi WPA2 handshake capture and password cracking",
        "goal": "crack WPA2 WiFi password by capturing and cracking the 4-way handshake",
        "keywords": ["aircrack-ng", "airmon-ng", "airodump-ng", "aireplay-ng", "handshake", "WPA2", "hashcat", "deauthentication"],
        "chain": ["put wireless adapter in monitor mode with airmon-ng",
                  "capture traffic and identify target AP with airodump-ng",
                  "send deauthentication frames to force handshake with aireplay-ng",
                  "capture 4-way WPA2 handshake",
                  "crack handshake with aircrack-ng or hashcat"],
    },
]
